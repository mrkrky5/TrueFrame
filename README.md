# True Frame

## Getting Started

## Commands

```powershell
npm run mobile:windows    # local preview (web frame)
npm run mobile:check      # validation
npm run mobile:sync       # sync shared data into mobile/
```

## Release

See `docs/eas-ready.md` for TestFlight / App Store steps (v2.0.0, build 17).
